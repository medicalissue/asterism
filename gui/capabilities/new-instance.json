{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "new-instance",
  "description": "What the New Instance window may reach. It listens for the progress events its own create emits, and closes itself when the user gives up. Everything else it does goes through the three commands in src/window.rs, which need no capability because the app defines them.",
  "windows": ["new-instance"],
  "permissions": [
    "core:event:default",
    "core:window:allow-close",
    "core:webview:allow-internal-toggle-devtools"
  ]
}
